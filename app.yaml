# app.yaml
runtime: python312 # Match your Python version

# Replace 'safe_mind' with your Django project's folder name
entrypoint: bash -c 'python manage.py migrate && gunicorn -b :$PORT safemind.wsgi'

handlers:
- url: /static
  static_dir: static/
- url: /.*
  script: auto